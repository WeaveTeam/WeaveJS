notifications:
  email: false
language: node_js
node_js:
  - 0.12
branches:
  only:
    - tests
before_install:
  - npm install grunt-cli -g
  - npm install
  - grunt
install: npm run compile
after_success:
  - chmod a+x printDir.sh
  - ./printDir.sh
